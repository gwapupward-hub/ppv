{
  "name": "gwap-ppv-protocol",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test:anchor": "ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts",
    "test:sdk": "npm --prefix sdk install && npm --prefix sdk test"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "0.30.1",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "chai": "^4.5.0",
    "mocha": "^11.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.7.0"
  }
}
