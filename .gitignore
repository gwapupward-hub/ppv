target/
.anchor/
.avm/
node_modules/
dist/
test-ledger/
.DS_Store
*.log
*.pem
*.key
*-keypair.json
# Solana program + deployer keypairs kept at repo root during a deploy.
# These are secret keys — never commit them.
*-program.json
*-deployer-*.json
.env
.env.*
!.env.example

